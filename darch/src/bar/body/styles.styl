@require "~styles/variables"
@require "~styles/utils"
@require "../styles"

.body
    position: relative;
    clearfix();

    @media (min-width: $screen-tablet-min)
        float: right

.body-background
    composes: background;

.closed
    // Closed only in small devices
    @media (max-width: $screen-phone-max)
        display: none;
